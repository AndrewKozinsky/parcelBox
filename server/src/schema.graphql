# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AdminOutModel {
  id: Int!
  email: String!
}

type SenderOutModel {
  id: Int!
  email: String!
  firstName: String
  lastName: String
  passportNum: String
  balance: Int!
  active: Boolean!
}

type Query {
  """User email confirmation"""
  auth_confirmEmail(input: ConfirmEmailInput!): Boolean!
}

input ConfirmEmailInput {
  """User email"""
  code: String!
}

type Mutation {
  """
  Register a user as a administrator.
  	Possible errors:
  	**Email is not confirmed** — the user is already registered, but doesn't confirm his email.
  	**Email is already registered** — the user is already registered and confirmed his email.
  """
  auth_RegisterAdmin(input: CreateAdminInput!): AdminOutModel!

  """
  Register a user as a administrator.
  	Possible errors:
  	**Email is not confirmed** — the user is already registered, but doesn't confirm his email.
  	**Email is already registered** — the user is already registered and confirmed his email.
  """
  auth_RegisterSender(input: CreateSenderInput!): SenderOutModel!
}

input CreateAdminInput {
  """User email"""
  email: String!

  """User password"""
  password: String!
}

input CreateSenderInput {
  """User email"""
  email: String!

  """User password"""
  password: String!
}